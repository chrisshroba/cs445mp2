close all;
[A, map] = imread('res/sepia.jpg');

% rng(1);

% imshow(A,map)
% quilt_simple(sample, outsize, patchsize, overlap, tol)
tic
Q = quilt_simple(A,[200 200], [25 25],5,);
% Q = quilt_cut(A,[300 300], [51 51],2, 3);

% Q = quilt_random(A,[900 900], [101 101]);

imshow(Q,map)

